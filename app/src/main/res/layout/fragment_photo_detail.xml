<!-- res/layout/fragment_photo_detail.xml -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- Top App Bar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:navigationIcon="@drawable/ic_arrow_back_24"
        app:title="Detalle de Foto"
        app:titleCentered="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <ImageView
        android:id="@+id/photo"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toTopOf="@+id/resultsContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintDimensionRatio="4:3"/>

    <com.chocoplot.apprecognicemedications.presentation.common.OverlayView
        android:id="@+id/overlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="@id/photo"
        app:layout_constraintBottom_toBottomOf="@id/photo"
        app:layout_constraintStart_toStartOf="@id/photo"
        app:layout_constraintEnd_toEndOf="@id/photo"/>

    <!-- Loading indicator -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/photo"
        app:layout_constraintBottom_toBottomOf="@id/photo"
        app:layout_constraintStart_toStartOf="@id/photo"
        app:layout_constraintEnd_toEndOf="@id/photo" />

    <!-- Manual detection button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnManualDetect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="Detectar medicamentos manualmente"
        app:layout_constraintBottom_toTopOf="@+id/resultsContainer"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_check_circle_24" />
        
    <!-- Inference time indicator -->
    <TextView
        android:id="@+id/inferenceTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:textColor="@android:color/white"
        android:background="@drawable/bg_inference_chip"
        android:padding="4dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/photo"
        app:layout_constraintEnd_toEndOf="@id/photo"
        tools:text="250ms" />

    <!-- Results Container -->
    <LinearLayout
        android:id="@+id/resultsContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="16dp"
        android:background="@drawable/bg_inference_chip"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHeight_max="200dp">

        <!-- Summary Header -->
        <LinearLayout
            android:id="@+id/summaryHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/summaryTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="📊 Detecciones de medicamentos"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textStyle="bold"
                android:ellipsize="none"
                android:scrollHorizontally="false"
                android:singleLine="false"
                android:breakStrategy="balanced"
                android:hyphenationFrequency="normal"
                android:lineSpacingExtra="2dp"
                android:justificationMode="inter_word"
                android:textColor="@android:color/white" />

            <TextView
                android:id="@+id/totalCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textStyle="bold"
                android:textColor="@android:color/white"
                tools:text="0 total" />

        </LinearLayout>

        <!-- Results List -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/resultsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxHeight="120dp"
            android:nestedScrollingEnabled="true"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/item_medication_result"
            tools:itemCount="3" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>